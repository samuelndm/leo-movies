import styled from "styled-components";
import * as GS from "assets/styles/GlobalStyles";

export const Container = styled(GS.ResponsiveContainer)`
  margin: 50px auto 0;
`;

export const Title = styled(GS.ContainerTitle)``;
